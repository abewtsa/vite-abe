const HeroCard = () => {
  return (
    <div className="hero-content-card">
      <h1>
        Test Content here, this should be a two line title, actually after
        thinking about it more, this will be 3 three lines section.
      </h1>
      <p>All things UX, UI and Front End.</p>
    </div>
  );
};

export default HeroCard;
